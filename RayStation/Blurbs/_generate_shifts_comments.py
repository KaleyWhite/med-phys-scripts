import os, sys

sys.path.append(os.path.join('T:', os.sep, 'Physics', 'KW', 'med-phys-scripts', 'RayStation'))
from generate_shifts_comments import generate_shifts_comments

generate_shifts_comments()
